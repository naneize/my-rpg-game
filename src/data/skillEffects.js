// data/skillEffects.js

/**
 * 🛡️ PASSIVE EFFECTS: ทำงานเมื่อ "โดนโจมตี"
 * รับค่าดาเมจปัจจุบันมาคำนวณ แล้วคืนค่าดาเมจใหม่กลับไป
 */
export const passiveEffects = {
  "Rubber Skin": (dmg) => Math.floor(dmg * 0.85),
  "Hard Shell": (dmg) => Math.floor(dmg * 0.70),
  "Iron Defense": (dmg) => Math.floor(dmg - 5), // หักดาเมจออกตรงๆ 5 หน่วย
  "Reflect": (dmg) => dmg, // เดี๋ยวไปเขียน Logic สะท้อนใน Hook อีกที
};

/**
 * ⚔️ ACTIVE/SPECIAL EFFECTS: ทำงานเมื่อ "โจมตีสวน"
 * รับค่าสเตตัสมาปรับแต่งก่อนคำนวณดาเมจ
 */
export const activeEffects = {
  "King Crush": (atk) => Math.floor(atk * 1.5),
  "Double Strike": (atk) => atk * 2,
  "Weak Spot": (atk) => atk + 10,
  "Sticky Slime": (atk) => Math.floor(atk * 1.5),
  "Jump Attack": (atk) => Math.floor(atk * 1.5)
};

/**
 * 🌟 SPECIAL EFFECTS: ท่าไม้ตายทำงานตามเงื่อนไขพิเศษ (เช่น HP < 20%)
 * สำหรับ Final Split จะเป็นการบัฟค่า Def
 */
export const specialEffects = {
  // รับค่าป้องกัน (def) มาคูณ 2 ตามเงื่อนไข
  "Final Split": (def) => def * 2,
  
  // แถม: เผื่อคุณอยากทำบอสตัวอื่นที่เลือดน้อยแล้วโจมตีแรงขึ้น 3 เท่า
  "Berserk": (atk) => atk * 3,
};

