// --- 📥 Import มอนสเตอร์จากแมพหลักอันเดียว (ที่มี 23 ตัว) ---
import { map1Monsters } from './map1_meadow'; 

/**
 * 🐉 Master Monster List (Single Map Edition)
 * ตอนนี้เราย้ายมอนสเตอร์ทุกตัวมาไว้ที่ Meadow เพื่อรองรับระบบแมพเดียว
 * และใช้ Biome Shift (เปลี่ยนโซนตามระยะก้าว) แทนการแยกไฟล์แมพ
 */
export const monsters = [
  ...map1Monsters,
  // ไฟล์อื่นๆ (map2 - map6) สามารถลบทิ้งหรือ Comment ออกได้เลยจ่ะ
];

// --- 💡 Tips การใช้งาน ---
// ตอนนี้ monsters จะมีมอนสเตอร์ทั้งหมด 23 ตัว 
// ระบบ useTravel และ CollectionView จะดึงจาก Array นี้ไปใช้โดยตรง